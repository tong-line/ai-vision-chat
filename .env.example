# 对话模式：mock（无 Key 演示）| direct（浏览器直连大模型）| proxy（走后端代理）
VITE_CHAT_MODE=mock

# 后端代理地址（proxy 模式使用，后续自行补充后端）
VITE_API_BASE_URL=http://localhost:3001

# 大模型配置（direct 模式使用，暂可不填）
VITE_LLM_API_KEY=
VITE_LLM_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
VITE_LLM_MODEL=qwen-vl-max
